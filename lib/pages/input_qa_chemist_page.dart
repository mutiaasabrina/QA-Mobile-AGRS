import 'package:flutter/material.dart';

class InputQAChemistPage extends StatelessWidget {
  const InputQAChemistPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("Input QA Chemist")),
      body: const Center(child: Text("Form Input QA Chemist di sini")),
);
}
}